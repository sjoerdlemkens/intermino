name: drift_notifications_api
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.1

dependencies:
  notifications_api:
    path: ../../../domain/abstract/notifications_api
  drift_app_database:
    path: ../../../infrastructure/data/db/drift_app_database

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
